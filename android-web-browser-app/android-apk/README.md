# Nova Android Browser APK

This Android project packages the web app from `../` into a native WebView APK.

Build command:

```powershell
gradle assembleDebug
```

APK output:

```text
app/build/outputs/apk/debug/app-debug.apk
```
